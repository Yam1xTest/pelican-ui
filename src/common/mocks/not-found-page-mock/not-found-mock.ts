import { BlockTypes } from "../../enum";
import { NotFoundComponentProps } from "../../types";

export const MOCK_NOT_FOUND: NotFoundComponentProps = {
  id: 1,
  __component: BlockTypes.NOT_FOUND,
};
